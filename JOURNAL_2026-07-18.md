# Journal de développement — 18 juillet 2026

Session de travail post-Lovable : autonomie complète + fiabilisation données + PWA + design.

## 1. Retrait complet de Lovable

| Fichier | Changement | Raison |
|---|---|---|
| `package.json` | suppression de `@lovable.dev/vite-tanstack-config` | dépendance propriétaire Lovable qui pilotait tout le build |
| `vite.config.ts` | réécriture complète | config Vite autonome (Tailwind v4, TanStack Start + `importProtection`, nitro build-only, React, alias `@`, injection `VITE_*`, déduplication React) |
| `src/lib/lovable-error-reporting.ts` | supprimé | reporting d'erreurs vers Lovable, inutile hors Lovable |
| `.lovable/` | supprimé | métadonnées Lovable |
| `src/routes/__root.tsx` | images OG `*.lovable.app` → `/og-image.png` local | les URLs de preview Lovable mourraient à terme |
| `src/integrations/supabase/*.ts` | messages « Connect Supabase in Lovable Cloud » → instructions neutres | cohérence |
| bonus | suppression de `vite-tsconfig-paths` | Vite 8 gère nativement les chemins tsconfig (`resolve.tsconfigPaths`) |
| `public/og-image.png` | créée | image de partage propre au projet |

## 2. Données & logique

### Nouveau : synchronisation des skills
- **Migration** `supabase/migrations/20260718000000_skill_states.sql` : table `skill_states(user_id, skill_id, status, note, updated_at)` + RLS.
  ⚠️ **À appliquer manuellement** (SQL Editor Supabase) — le code tolère son absence.
- `store.ts` : la synchro globale fusionne désormais `skillNotes`/`skillStatuses` avec Supabase (le distant gagne les conflits, les entrées locales nouvelles sont poussées). Le statut `auto` (calculé) n'est jamais persisté.
- Actions `setSkillNote` / `setSkillStatus` : upsert immédiat + suppression de la ligne quand il n'y a plus rien à stocker.

### Correctifs TypeScript (existant avant, jamais détectés car le build ne type-check pas)
| Erreur | Cause | Correctif |
|---|---|---|
| **Toutes les requêtes Supabase typées `never`** | `types.ts` généré dans un ancien format : `referencedSchema` au lieu de `referencedColumns` exigé par `GenericRelationship` (postgrest-js 2.110) | les 10 entrées `Relationships` corrigées → autocomplétion IntelliSense restaurée dans VS Code |
| `BottomNav.tsx` ×2 (TS2367) | branches mortes comparant à `"/"`, absent de l'union des routes du menu « Plus » | code mort supprimé |
| `store.ts` cast `Json → SetLog[]` | cast trop strict pour le typage réel (révélé une fois les types réparés) | `as unknown as SetLog[]` (pattern déjà utilisé ailleurs) |

Résultat : `npx tsc --noEmit` **0 erreur** (état vérifié aussi avant toute modification : ces erreurs pré-existaient).

## 3. PWA (installable + hors-ligne)

- `public/manifest.webmanifest` : nom, `display: standalone`, portrait, couleurs `#05070f`, icônes.
- Icônes générées : `icon-192/512`, `icon-maskable-512` (padding adapté), `apple-touch-icon` (180), `favicon-32` ; source 1024 dans `docs/design/`.
- `public/sw.js` : service worker écrit à la main —
  - pages HTML : réseau d'abord, repli cache (mode hors-ligne) ;
  - assets hashés Vite : cache d'abord ;
  - **aucune** interception des appels API externes (Supabase/auth jamais cachés).
- Enregistrement dans `__root.tsx` (prod uniquement, `import.meta.env.DEV` ignoré).
- Balises meta : `theme-color` alignée sur le fond réel (`#05070f`), `apple-mobile-web-app-*`.

## 4. Design premium (additif, zéro casse)

- `styles.css` : keyframes `fade-up`/`shimmer`, utilities `animate-fade-up`/`animate-shimmer`, `.page-enter`, sélection violette, scrollbar discrète, `focus-visible` cohérent, **respect de `prefers-reduced-motion`**, lissage des polices.
- `__root.tsx` : transition de page fondue à chaque navigation (`key` sur le pathname).
- `BottomNav` : point lumineux (glow primaire) sous l'onglet actif, y compris le bouton « Plus ».
- `TopBar` : barre de dégradé accentuée devant les titres de page.

## Tests effectués

- `npm run build` : ✅ (client + SSR + nitro)
- `npx tsc --noEmit` : ✅ 0 erreur
- `npm run lint` : ✅ 0 erreur (6 warnings `react-refresh` inoffensifs, pré-existants)
- `vite dev` + curl : HTTP 200 sur `/`, `/connexion`, `/skills`

## Tests manuels à faire

- Appliquer la migration skill_states puis **Tester la synchro skills** sur 2 appareils/navigateurs.
- Vérifier l'installation PWA sur mobile (Android : menu ⋮ → « Installer » ; iOS : partager → « Sur l'écran d'accueil »).
- Vérifier le mode hors-ligne après une première visite.
- Sur Vercel : confirmer les variables d'environnement `VITE_SUPABASE_*` (déjà nécessaires avant cette session).

## Risques

- Faible : transitions de page remontant le contenu — aucun état n'est conservé entre routes par design (comportement router standard).
- Faible : SW en prod — invalider proprement (changer `VERSION` dans `sw.js`) lors de changements majeurs de stratégie de cache.

## 5. Correctif mobile — labels de la barre de navigation (18/07, suite)

- `BottomNav.tsx` : « Progression » → « Progrès » (trop long, débordait sur « Nutrition » aux petites largeurs d'écran / zoom d'affichage)
- Anti-chevauchement permanent : `min-w-0` sur les `<li>`, `truncate` + `px-0.5` sur les labels → un label ne peut plus jamais empiéter sur son voisin, quelle que soit la largeur d'écran.
- Validé : tsc ✅ lint ✅ build ✅

## 6. Refonte visuelle "Coral Glass" (validée par l'utilisateur via design board)

Direction : accent principal dégradé **corail → rose**, couleur par catégorie de stat, fond nuit **marbré**, cartes verre dépoli XXL.
Inspirations utilisateur : app running (panel corail), dashboard (couleurs par stat), wellness (capsules), smoothie/travel (rayons XXL).

| Élément | Implémentation |
|---|---|
| Palette corail | `--primary: #ff6b4a`, `--gradient-primary` corail→rose, ring/glow/selection/scrollbar harmonisés (`styles.css`) |
| Fond marbré | grand halo dégradé doux (voile mauve + lueurs corail/rose) en couches fixes sur `body` (texture marbre retirée à la demande) |
| Panel héros accueil | nouvelle utility `panel-coral` (verre dépoli sur dégradé corail) pour la carte "Séance du jour" |
| Stats par catégorie | Protéines = lime, Eau = cyan (cartes teintées + barres glow) sur `index.tsx` et `nutrition.tsx` |
| **Bouteille d'eau** | nouveau composant `src/components/WaterBottle.tsx` (SVG pur, niveau animé, doubles vagues, glow + bouchon cyan à 100%), +25cl/+50cl conservés |
| og-image | régénérée aux couleurs corail |

Validé : tsc ✅ lint ✅ build ✅ dev (/, /nutrition, /marble.jpg → 200) ✅

## 7. Feature : outil quotidien + plan personnalisé généré (demande utilisateur)

L'app n'est plus « 12 semaines seulement » → cycles infinis + onboarding générateur de plan.

| Élément | Détail |
|---|---|
| **Cycles infinis** | `programCycle()` : S13 = Cycle 2 · Sem 1… plus de plafond à 12 (tests toutes les 4 semaines par cycle) |
| **Onboarding `/onboarding`** | wizard 4 étapes (Objectif → Corps → Capacités → Plan généré), garde-fou dans `__root.tsx` : profil non configuré → redirection auto |
| **Moteur `src/lib/plan.ts`** | 4 objectifs (recomp / sèche / muscle / skills), palier 1-3 (niveau + capacités déclarées, facto ×0.7/×1/×1.3 sur les fourchettes), nutrition Mifflin-St Jeor (kcal par objectif, protéines g/kg, eau 35ml/kg) |
| **Plan appliqué partout** | `planDays()` alimente séance, accueil, programme (fourchettes adaptées) ; `nutritionTargets()` alimente accueil et nutrition (remplace les constantes fixes) |
| **Régénération** | bouton « Générer un nouveau plan » dans Paramètres (ne réinitialise PAS le cycle) |
| **BDD** | migration `20260718000001_profile_plan.sql` : `profiles.age`, `sex`, `capacities`, `plan` (JSONB) ⚠️ à exécuter dans le SQL Editor |

## 8. Hotfix : bouton « Continuer » bloqué dans l'onboarding

La `BottomNav` (z-40, rendue après l'Outlet) recouvrait la barre de navigation de l'onboarding
(également fixe, z-40) → bouton Continuer/Retour incliquable. Fix : `BottomNav` retourne `null`
sur `/onboarding` (assistant plein écran, meilleure UX au passage).

## 9. Fix : « Générer un nouveau plan » sans effet

Le garde-fou de `__root.tsx` redirigeait tout utilisateur onboardé de `/onboarding` vers `/` →
le lien de `parametres.tsx` semblait mort. Branche supprimée : l'onboarding reste accessible
pour la régénération (il conserve `startDate` / le cycle).

## 10. V2 — Le plan qui s'adapte vraiment (10 chantiers validés par l'utilisateur)

| Élément | Détail |
|---|---|
| **Glucides & lipides** | `computeNutrition` ajoute `carbsTarget` (kcal restantes) + `fatTarget` (~0.9 g/kg) ; affichés dans Nutrition et l'onboarding |
| **Cibles sur pesée** | `nutritionTargets()` recalcule toujours (déterministe) ; Mesures propose « Mettre à jour » quand le poids saisi ≠ profil → cibles auto-recalculées |
| **Palier sur perfs réelles** | `capacitiesFromHistory()` (max tests + meilleures séries) pré-remplit l'onboarding ; fallback valeurs déclarées au 1er cycle (consigne utilisateur) |
| **Bilan fin de cycle** | carte « Fin du cycle 🎉 » sur l'accueil en semaine 12 → régénération |
| **Suggestions dans la séance** | sous chaque exo : « ↺ Dernière : 8·8·7 · RPE 9 · 12 juil. » + chip suggestion |
| **Suggestions V2 (2 séances)** | validé 2 fois → +2 reps/+8s ; 2 échecs → reason `down` (-1 rep / -5s) pour repartir propre |
| **Jours d'entraînement** | chips Lun→Dim dans l'onboarding (3-6 j) ; `trainingDays` (JSONB) ; `applyTrainingDays()` transforme les autres en repos (séance fixe par week-day, prévisible) ; `plannedSessionsPerWeek()` |
| **Remplacement d'exo** | table `EXERCISE_SWAPS` (30+ exos, régressions/équivalents) ; sélecteur dans la carte exo ; persisté `exerciseSwaps` (`dayKey::exId`) |
| **Semaine visuelle** | pastilles L→D sur l'accueil (✓ fait / emoji prévu / · repos) |
| **Bilan hebdo** | dimanche (semaine en cours) + lundi (semaine écoulée) : séances/km/prot/eau moyennes + Δ poids + 1 conseil ciblé ; masquable (`localStorage`) |
| **Records personnels** | `personalBests()` + toast « 🏆 » à l'enregistrement d'une séance qui bat un record ; section Records dans Progression |
| **BDD** | migration `20260718000002_profile_v2.sql` : `profiles.training_days`, `exercise_swaps` (JSONB) ⚠️ à exécuter |

Validé : tsc ✅ eslint ✅ build ✅ dev (/, /seance, /nutrition, /onboarding, /progression, /mesures, /programme, /parametres → 200) ✅
Patch : `v2-plan-adaptatif.patch` (journal exclu — absent du poste utilisateur).

## 11. V3 — Hors-ligne & rappels locaux

| Élément | Détail |
|---|---|
| **Synchro auto au retour réseau** | listener `online` dans `__root.tsx` → `syncAllDataWithSupabase()` (la réconciliation existante repousse les écritures locales — aucune perte) |
| **Bandeau réseau** | nouveau `src/components/OfflineBanner.tsx` (ambre hors-ligne rassurant · vert 5 s à la reconnexion), rendu dans `__root.tsx` |
| **Toasts hors-ligne** | `syncFailureToast()` mutualisé : mode hors-ligne → toast info « 💾 gardé sur l'appareil… » au lieu d'erreur (9 appels) |
| **Rappels locaux** | nouveau `src/lib/reminders.ts` : réglages par appareil (localStorage), boucle 60 s + rattrapage à l'ouverture, notification native si permission sinon toast in-app, 1×/jour/type |
| **Carte Rappels** | Paramètres : rappel séance (heure au choix, si pas entraîné) + hydratation (15h si < 50 % de la cible) + gestion de la permission (iPhone → PWA installée) |
| **sw.js** | VERSION v1 → v2 (rafraîchit le cache au déploiement) |
| **BDD** | aucune migration nécessaire 🎉 |

Validé : tsc ✅ eslint ✅ build ✅ dev (/, /seance, /parametres, /onboarding → 200) ✅
Patch : `v3-hors-ligne-rappels.patch`.

### Feuille de route annoncée par l'utilisateur
- **V4** : vue adaptée iPhone/smartphone + vue adaptée Mac/PC Windows (layout desktop)
- **V4/V5** : export des données vers son propre **Notion** (API publique Notion, clé perso)
- Option à étudier : vraies notifications push (web-push + cron gratuit, nécessite ~10 min de config Vercel/Supabase)

## 12. V4 — Vue desktop & export/Notion (validée par l'utilisateur)

| Élément | Détail |
|---|---|
| **Sidebar desktop** | `DesktopNav` (BottomNav.tsx) : icônes seules en lg (≥1024px), label en xl (≥1280px), streak + email en pied ; masquée mobile + /onboarding ; `BottomNav` = `lg:hidden` |
| **PageShell responsive** | à gauche de la sidebar (lg:pl-20 xl:pl-64), contenu max-w-6xl centré, prop `className` (twMerge) |
| **Layouts par page** | Accueil 3 col · Séance exos 2 col (logique intouchée) · Programme/Historique/Skills/Mesures 2 col · Tests 2→3 col · Nutrition 2 col (stats+aujourd'hui | favoris+templates+perso) · Paramètres 2 col |
| **Raccourcis PC** | Espace = valider la série suivante · Échap = fermer le minuteur (hors champs) |
| **Export fichiers 14 j** | `src/lib/exporter.ts` : fenêtre glissante 14 j (rythme pesée, demande utilisateur) ; CSV (RFC, virgules) + Markdown ; 4 datasets (séances/mesures/tests/repas) + ID stable anti-doublons ; guide d'import Notion intégré (Paramètres) |
| **Synchro Notion auto** | `notion-sync.ts` = `createServerFn` (RPC serveur Vercel — contourne CORS sans config) : crée les 4 bases sous la page parente, dédup par propriété ID, ≤40 créations/appel, throttle 340 ms ; client `notion.ts` (réglages localStorage ; clé jamais stockée serveur) |
| **Export PDF semaine** | bouton 🖨️ sur /programme → `window.print()` ; feuille imprimable cachée (`print:` Tailwind + CSS @media print) |
| **Favoris repas** | `Profile.favoriteMeals` (JSONB, migration #5) ; ⭐ sur chaque repas du jour ; section favoris avec ré-ajout 1 tap |
| **BDD** | migration `20260719000001_favorite_meals.sql` ⚠️ à exécuter |

Validé : tsc ✅ eslint ✅ build ✅ dev (10 routes → 200) ✅
Patch : `v4-desktop-notion.patch`.

## 12. V4 — Vue desktop & export/Notion (validée par l'utilisateur)

| Élément | Détail |
|---|---|
| **Sidebar desktop** | `DesktopNav` (BottomNav.tsx) : icônes seules en lg (≥1024px), labels en xl (≥1280px), streak + email en pied ; masquée mobile + /onboarding ; `BottomNav` = `lg:hidden` |
| **PageShell responsive** | à droite de la sidebar (lg:pl-20 xl:pl-64), contenu max-w-6xl centré, prop `className` (twMerge) |
| **Layouts par page** | Accueil 3 col · Séance exos 2 col (logique intouchée) · Programme/Historique/Skills/Mesures 2 col · Tests 2→3 col · Nutrition 2 col · Paramètres 2 col |
| **Raccourcis PC** | Espace = valider la série suivante · Échap = fermer le minuteur (ignorés dans les champs) |
| **Export fichiers 14 j** | `exporter.ts` : fenêtre glissante 14 j (rythme pesée) ; CSV + Markdown ; 4 datasets + ID stable anti-doublons ; guide d'import Notion intégré |
| **Synchro Notion auto** | `notion-sync.ts` (`createServerFn` → RPC serveur Vercel, contourne CORS sans config) : crée les 4 bases sous ta page, dédup par ID, ≤40 créations/appel, throttle 340 ms ; client `notion.ts` (réglages localStorage, clé jamais stockée serveur) |
| **Export PDF semaine** | bouton 🖨️ sur /programme → `window.print()` (feuille imprimable `print:` Tailwind + @media print) |
| **Favoris repas** | `Profile.favoriteMeals` (JSONB, migration #5) ; ⭐ sur chaque repas ; ré-ajout 1 tap |
| **BDD** | migration `20260719000001_favorite_meals.sql` ⚠️ à exécuter |

Validé : tsc ✅ eslint ✅ build ✅ dev (10 routes → 200) ✅
Patch : `v4-desktop-notion.patch`.

## 13. V5 — Synchro Notion adaptative (19 juillet aprem)

Demande utilisateur : la synchro doit s'adapter au Notion de chacun (il a 4 bases
existantes : suivi quotidien séances, suivi hebdo, macros quotidiennes, vue mensuelle).
Réponses : ses stats sont des formules (on n'écrit que les nombres), titres en mention
date « @… », plan validé en entier (10 points).

- `notion-sync.ts` : réécrit en RELAIS GÉNÉRIQUE (`notionRequest`) — toute la logique
  passe côté app ; le serveur ne fait que forwarder (CORS). Corps renvoyé en JSON
  stringifié (contrainte de sérialisation TanStack Start).
- `notion-datasets.ts` (nouveau) : catalogue de 6 jeux de données avec champs typés +
  moteur d'agrégats par période :
  - « Séances du jour » (muscu + cardio fusionnés ; meilleure série pompes/tractions/
    dips/squats, gainage max, km/chrono/allure course, résumé texte)
  - « Résumé hebdo » (lundi → séances, km, durée, kcal moy)
  - « Macros du jour » (totaux + cibles recalculées du plan, écart, résumé)
  - « Résumé mensuel » (poids début/fin, kcal cible/moy, rigueur %, séances, km)
  - « Mesures » + « Tests » (par élément). Fenêtre 14 j (mois/semaine calculés sur
    tout l'historique local). `guessProperty` pré-remplit le mapping (hints FR).
- `notion.ts` : réglages V2 (par export : off / auto / existing + URL base + mapping),
  migration V1→V2 (bases auto séances/mesures/tests conservées, « repas » → « macros »),
  introspection `GET /v1/databases/:id` (colonnes + types + selects), `addUidColumn`
  (PATCH database), upsert : 1 ligne/jour/semaine/mois retrouvée par la colonne Date
  (query filtre `on_or_after`) ou par colonne ID pour les tests ; écriture =
  PATCH si trouvée, POST sinon ; JAMAIS d'écriture sur les champs non mappés
  (RPE, notes, formules intouchés) ; titres en mention date (suffixe texte possible)
  ou texte simple ; throttle 350 ms ; rapport détaillé par jeu.
- `parametres.tsx` : carte Notion repensée — choix du mode par export, URL + bouton
  « Analyser », correspondances champ→colonne filtrées par type compatible, bouton
  « ajouter la colonne ID » (tests), style de titre, rapport de synchro affiché.
- Profil : `notion_config` JSONB (migration 20260719000002) + 4 points de mapping
  store + types — config retrouvée sur tous les appareils (adoption auto sur appareil
  vierge). sw.js v2 → v3.

Attention : relayer que la clé Notion est désormais stockée dans SON profil Supabase
(RLS), c'est le prix du multi-appareils — mentionné dans l'UI.

## 14. V6 — Notion liste dynamique + rappel export + fix layout (19 juillet soir)

Retours utilisateur : (1) bloc Notion = 1 base + bouton ajouter ; (2) nom et
propriétés affichés = ceux de la VRAIE base (fini les champs fantômes du gabarit) ;
(3) rappel d'export ; (4) gros trous entre blocs desktop + chevauchement
Protéines/semaine sur Aujourd'hui.

- Layout : utilitaires CSS `.masonry-lg` (+`.masonry-xl-3`) dans styles.css —
  multi-colonnes CSS avec break-inside-avoid + marges enfants. Toutes les grilles
  desktop avec placements manuels (row-start/col-start) remplacées : index,
  programme, mesures, nutrition, parametres, progression, historique, skills.
  Mobile inchangé (règles scoped @media ≥1024px). Fin des trous + chevauchements.
- Notion V6 : modèle `LinkedBase[]` (id, dataset, mode, databaseUrl, mapping,
  autoDbId, knownName) remplace l'objet par dataset ; migrations V4→V5→V6 dans
  loadNotionSettings (clé localStorage inchangée, profil notion_config réutilisé —
  PAS de nouvelle migration SQL). syncToNotion itère sur la liste ; libellés de
  rapport = vrai nom de base quand connu.
- Carte Notion repensée : liste dynamique (➕ Ajouter une base, 🗑️ retirer),
  entête = vrai nom après Analyser ; tableau de correspondance PILOTÉ PAR LES
  COLONNES RÉELLES de la base (chaque colonne mappable → donnée de l'app ou
  « Ne pas remplir ») ; colonnes non remplissables (formules/relations) listées
  comme ignorées ; champs requis (Date, ID tests) épinglés avec alerte ; 1 champ =
  1 colonne max (réaffectation automatique) ; changement de dataset = re-préremplissage.
- Rappel export : reminders.ts + ReminderSettings(exportReminder, exportDay,
  exportAnchor) — toutes les 2 semaines le jour choisi, à partir de 9h, 1×/jour ;
  UI dans la carte Rappels (sélecteur jour + switch ; ancre posée à l'activation).

Validé : tsc, eslint (6 warnings shadcn préexistants), build, smoke test 5173 (8 routes 200).
Aucune migration SQL nécessaire pour passation (colonne notion_config de V5 réutilisée).

## 15. V7 — Compte, détection Notion auto, œil clé, fix Aujourd'hui (19 juillet nuit)

Retours utilisateur : (1) blocs sous le bandeau semaine collés (desktop) ;
(2) supprimer le sélecteur de template Notion — tout doit venir de la base ;
(3) œil voir/cacher la clé ; (4) vraie section Compte (username, mot de passe) ;
(5) paramètres en sous-sections.

- index.tsx : `lg:mt-0` retiré des 4 sections masonry → marge de 24 px rétablie
  sous le bandeau semaine (le bug venait de la V6 qui héritait ces classes de la
  grille V4). Autres pages OK.
- `detectDataset` (notion-datasets.ts) : score par base = nb de hints de champs
  trouvés dans les noms de colonnes (+2 si le NOM de la base colle, ex. « hebdo »,
  « macro », « mensuel »). Test unitaire tsx sur les 4 bases réelles de
  l'utilisateur : seances(7) / hebdo(6) / macros(9) / mensuel(9), toutes
  « confident » ✅. Sélecteur de template SUPPRIMÉ du UI (restant uniquement en
  mode « base auto », nécessaire pour savoir quoi créer). Badge « 🤖 détecté auto ».
- Carte Notion : bouton 👁️ sur la clé d'intégration (Eye/EyeOff).
- Paramètres réorganisés en sous-sections titrées (SectionTitle) avec un
  masonry par section (titres hors flux multi-colonnes) : 👤 Compte (AccountCard
  + Rappels) / 🏋️ Profil (profil, fréquence, niveau, équipement, nouveau plan) /
  📓 Notion & données (export + synchro) / 🔗 Raccourcis & divers (liens, zone
  danger avec bouton destructive, version).
- AccountCard : email (lecture seule), nom d'utilisateur éditable (profil,
  nouveau champ SQL `username` — migration 20260719000003 + 4 points de mapping
  store + types ×3), affiché aussi dans DesktopNav (« 👤 nom · email ») ;
  changement de mot de passe via supabase.auth.updateUser (2 champs + valid.
  6 min + correspondance) ; déconnexion déplacée ici.

Validé : tsc, eslint (6 warnings shadcn), build, smoke 5173, test détection.
SQL à donner : alter table public.profiles add column if not exists username text;

## 16. V8 — Accueil personnalisable, filigrane TEST, suppression séance, compte

- **Renommage** : entrée de nav « Aujourd'hui » → « Accueil » (BottomNav label + titre de page index).
- **Accueil personnalisable** (grosse pièce) : nouveau modèle `src/lib/home-layout.ts`
  (HomeSection → HomeBlockInstance, 7 types de blocs : seance/nutrition/regles/liens/bilan/cycle/testsBanner,
  span 1-3). `normalizeHomeLayout()` ne réinjecte JAMAIS les blocs retirés par l'utilisateur.
  index.tsx restructuré : grille `lg:grid-cols-3 lg:grid-flow-dense` par section (remplace le masonry CSS
  qui tronquait les margin-top en haut de colonnes 2/3 → fini les chevauchements Règles d'or / Séance du jour),
  mode ✏️ Personnaliser (déplacer ↑↓, redimensionner −/+, retirer 🗑️, ajouter via Select,
  sous-sections renommables/supprimables avec filet de sécurité « dernière section », reset avec confirm).
  Sauvegarde dans `profiles.home_layout` (JSONB) → suit l'utilisateur sur tous ses appareils.
- **Migration #8** `20260719000004_home_layout.sql` : `home_layout jsonb not null default '[]'`.
- **store.ts** : 4 points de mapping home_layout (2 lectures + 2 upserts) + types.ts Row/Insert/Update ;
  nouvelle action `removeWorkout` (suppression locale D'ABORD pour empêcher la synchro de réconciliation
  de faire « ressusciter » la séance, puis delete Supabase exercise_logs + workout_sessions filtré user_id).
- **Programme** : filigrane « TEST » diagonal très discret (rotation -12°, text-white/15) dans les cases
  dorées S4/S8/S12 du Plan 12 semaines.
- **Historique** : bouton 🗑️ sur chaque carte séance (confirm → removeWorkout + retrait local de la liste).
- **Paramètres** : « Compte connecté » affiche le pseudo (email en petit dessous), lien historique
  n'est plus rouge (text-primary retiré), Objectif = Select avec GOALS (⚖️/🔥/💪/🤸) au lieu d'un texte libre.
- Validations : tsc clean, eslint 0 erreur (6 warnings shadcn pré-existants), build OK, smoke test 200.

## 17. V9 — Widgets façon Apple, nouveaux blocs, évolution skills, menu mobile

- **Accueil** : en-tête affiche le pseudo (« Connecté : louis ») ; personnalisation repensée
  façon widgets Apple — **glisser-déposer** des blocs (Pointer Events : tactile + souris, cible
  surlignée pendant le drag, insertion avant/après selon la moitié du bloc, cross-sous-sections)
  et **redimensionnement à la poignée** coin bas-droit (~110 px tirés = 1 colonne).
  Bouton **« + Bloc »** (à côté de « Sous-section ») : panneau avec TOUS les blocs possibles —
  nouveaux : 🔥 Kcal, 🍞 Glucides, 🥑 Lipides (compteurs du jour vs cibles du plan),
  🏆 Mes skills (vue d'ensemble), 🎯 Un skill (carte dédiée MULTI-instances via refId, skill
  choisi dans le bloc en mode édition), 🔄 Synchroniser mes données (lance syncToNotion),
  📏 Mes mesures (dernier poids/taille/sommeil), 🎵 Musique (coquille vide → V10).
- **Évolution des skills** : nouveau composant `EvolutionChart.tsx` (courbe SVG maison, zéro
  dépendance — record en ambre, dates début/fin, repère « plus bas = mieux » pour le 5 km) ;
  bouton « Voir l'évolution » dans Progrès (chaque test) ET Skills (chaque skill).
- **Progrès** : explication du champ « Valeur » adaptée au test choisi (s / reps / min).
- **Programme** : filigrane TEST agrandi (clamp 14→30 px, inset 1.5 → jamais contre les bords).
- **Mesures** : photo Face/Profil/Dos → panneau de choix 📸 caméra (capture) / 🖼️ photothèque /
  📁 fichiers ; explication sous le champ Sommeil (« nuit dernière, pas de moyenne »).
- **Notion** : synchro à période choisie — 14 j / semaine dernière / 2 dernières semaines
  (semaines complètes L→D) / mois au choix / personnalisé (dates) — + cases pour ne synchroniser
  que certaines bases (refactor buildNotionRows(state, range?) sur les 6 agrégats,
  syncToNotion(state, onProgress, {range, onlyBases})).
- **Menu mobile personnalisable** : NAV_CANDIDATES dans `lib/nav-menu.ts` (8 menus), 3 à choisir
  dans Paramètres (Accueil & Plus fixes, max 3, min 1), sauvegardé dans profiles.nav_menus
  (migration #9). DesktopNav garde tous les menus.
- **Refacto** : computeAutoStatus déplacé dans `lib/skill-status.ts` (partagé Skills + Accueil).
- Validations : tsc clean, eslint 0 erreur (6 warnings shadcn pré-existants), build OK, 5 routes 200.
