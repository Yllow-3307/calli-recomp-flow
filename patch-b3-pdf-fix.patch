diff --git a/src/lib/exporter.ts b/src/lib/exporter.ts
index a0b73bf..f00b6c5 100644
--- a/src/lib/exporter.ts
+++ b/src/lib/exporter.ts
@@ -204,7 +204,6 @@ export async function exportProgramPDF(): Promise<boolean> {
     const el = document.getElementById("program-pdf-content");
     if (!el) {
       console.error("PDF: élément #program-pdf-content introuvable");
-      // Fallback: impression navigateur stylée
       window.print();
       return true;
     }
@@ -212,12 +211,22 @@ export async function exportProgramPDF(): Promise<boolean> {
     const { default: html2canvas } = await import("html2canvas");
     const { default: jsPDF } = await import("jspdf");
 
-    // Cloner l'élément pour éviter les artefacts
+    // Cloner l'élément et neutraliser les styles qui cassent html2canvas
     const clone = el.cloneNode(true) as HTMLElement;
     clone.style.padding = "20px";
     clone.style.background = "#ffffff";
     clone.style.color = "#000000";
     clone.style.width = "800px";
+    // Neutraliser backdrop-filter (cause de crash html2canvas)
+    const all = clone.querySelectorAll("*");
+    all.forEach((node) => {
+      const el2 = node as HTMLElement;
+      el2.style.backdropFilter = "none";
+      el2.style.webkitBackdropFilter = "none";
+      el2.style.backgroundImage = "none";
+      el2.style.background = "#ffffff";
+      el2.style.color = "#000000";
+    });
     
     // Ajouter temporairement au DOM
     const wrapper = document.createElement("div");
@@ -245,11 +254,9 @@ export async function exportProgramPDF(): Promise<boolean> {
     const imgW = pageW - 15;
     const imgH = (canvas.height * imgW) / canvas.width;
 
-    // Si tient sur une page
     if (imgH <= pageH - 15) {
       pdf.addImage(imgData, "JPEG", 7.5, 7.5, imgW, imgH);
     } else {
-      // Multi-pages
       pdf.addImage(imgData, "JPEG", 7.5, 7.5, imgW, imgH);
       let remaining = imgH - (pageH - 15);
       let offset = pageH - 15;
@@ -265,8 +272,9 @@ export async function exportProgramPDF(): Promise<boolean> {
     pdf.save(filename);
     return true;
   } catch (err) {
-    console.error("Erreur export PDF:", err);
-    // Fallback ultime: impression navigateur
+    const msg = err instanceof Error ? err.message : String(err);
+    console.error("Erreur export PDF:", msg);
+    // Fallback: impression navigateur + toast
     try {
       window.print();
       return true;
